import styled from "styled-components";

const ToolbarContainer = styled.div`
  padding: 1rem;
  border-bottom: 1px solid #dadce0;
  font-weight: 700;
`;

function Toolbar() {
  return <ToolbarContainer>성경 1독</ToolbarContainer>;
}

export default Toolbar;
